[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node start-railway-complete.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[build.nixpacks]
buildCommand = "npm install"

[environments.production]
NODE_ENV = "production"
PORT = 10000