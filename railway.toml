[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && npm install && npm run build && node start.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production]
NODE_ENV = "production"
PORT = "3001"