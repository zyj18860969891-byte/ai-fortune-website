[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node dist/simple-test-server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[build]
builder = "NIXPACKS"

[build.nixpacks]
buildCommand = "cd backend && npm install && echo 'Build completed'"

[environments.production]
NODE_ENV = "production"



[environments.production]
NODE_ENV = "production"

[build.nixpacks]
buildCommand = "cd backend && npm install && echo 'Build skipped for backend-only deployment'"



