# 📤 手动推送代码到GitHub指南

## 方法1: 使用GitHub Desktop (推荐)

1. **下载GitHub Desktop**
   - 访问: https://desktop.github.com/
   - 下载并安装

2. **克隆仓库**
   - 打开GitHub Desktop
   - File → Clone Repository
   - 选择: `ai-fortune-website`
   - 本地路径: `E:\MultiModel\ai-fortune-website`

3. **推送更改**
   - GitHub Desktop会自动检测到更改
   - 点击"Push origin"按钮
   - 等待推送完成

## 方法2: 使用Git命令行

```bash
# 进入项目目录
cd E:\MultiModel\ai-fortune-website

# 设置GitHub凭据 (如果需要)
git config --global user.name "你的用户名"
git config --global user.email "你的邮箱"

# 推送代码
git push origin master
```

## 方法3: 使用VS Code

1. **打开项目**
   - 在VS Code中打开 `E:\MultiModel\ai-fortune-website`

2. **使用Git扩展**
   - 点击左侧Git图标
   - 点击"推送"按钮
   - 输入GitHub凭据

## 方法4: 直接上传文件

如果推送仍有问题，可以：

1. **打包项目文件**
   - 压缩整个 `ai-fortune-website` 文件夹
   - 上传到云盘分享

2. **手动创建新仓库**
   - 在GitHub创建新仓库
   - 上传本地文件

## 推送后的验证

推送成功后，检查：
- [ ] GitHub仓库显示最新提交
- [ ] 包含 `frontend/` 目录
- [ ] 包含 `vercel.json` 配置文件
- [ ] 包含 `.env.production` 文件

---

💡 **提示**: 如果网络问题持续，建议使用GitHub Desktop，它通常更稳定。